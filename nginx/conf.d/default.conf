# GitLab - HTTP to HTTPS redirect
server {
    listen 80;
    server_name gitlab.dunnoda-demo.dev;
    return 301 https://$server_name$request_uri;
}

# GitLab - HTTPS
server {
    listen 443 ssl;
    server_name gitlab.dunnoda-demo.dev;

    ssl_certificate /etc/nginx/ssl/gitlab/gitlab.dunnoda-demo.dev.crt;
    ssl_certificate_key /etc/nginx/ssl/gitlab/gitlab.dunnoda-demo.dev.key;
    
    # Modern SSL configuration
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers ECDHE-RSA-AES128-GCM-SHA256:ECDHE-RSA-AES256-GCM-SHA384;
    ssl_prefer_server_ciphers off;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 10m;

    client_max_body_size 250m;

    location / {
        proxy_pass http://gitlab;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 300s;
        proxy_connect_timeout 75s;
    }
}

# Wiki.js - HTTP to HTTPS redirect
server {
    listen 80;
    server_name wiki.dunnoda-demo.dev;
    return 301 https://$server_name$request_uri;
}

# Wiki.js - HTTPS
server {
    listen 443 ssl;
    server_name wiki.dunnoda-demo.dev;

    ssl_certificate /etc/nginx/ssl/wiki/wiki.dunnoda-demo.dev.crt;
    ssl_certificate_key /etc/nginx/ssl/wiki/wiki.dunnoda-demo.dev.key;
    
    # Modern SSL configuration
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers ECDHE-RSA-AES128-GCM-SHA256:ECDHE-RSA-AES256-GCM-SHA384;
    ssl_prefer_server_ciphers off;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 10m;

    location / {
        proxy_pass http://wikijs:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# OpenProject - HTTP to HTTPS redirect
server {
    listen 80;
    server_name project.dunnoda-demo.dev;
    return 301 https://$server_name$request_uri;
}

# OpenProject - HTTPS
server {
    listen 443 ssl;
    server_name project.dunnoda-demo.dev;

    ssl_certificate /etc/nginx/ssl/project/project.dunnoda-demo.dev.crt;
    ssl_certificate_key /etc/nginx/ssl/project/project.dunnoda-demo.dev.key;
    
    # Modern SSL configuration
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers ECDHE-RSA-AES128-GCM-SHA256:ECDHE-RSA-AES256-GCM-SHA384;
    ssl_prefer_server_ciphers off;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 10m;

    client_max_body_size 100m;

    location / {
        proxy_pass http://openproject-web:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 300s;
    }
}
